all:
  hosts:
    edge-server:
      ansible_host: localhost        # Your actual server IP
      ansible_port: 2222
      ansible_user: ubuntu              # Your SSH user
      ansible_ssh_private_key_file: ~/.ssh/ansible_test_key  # Your SSH key
  vars:
    ansible_python_interpreter: /usr/bin/python3